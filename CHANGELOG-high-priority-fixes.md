# แก้ไขตามข้อเสนอแนะระดับ "สูง" ใน design-checklist-activity9-section3-5.md

อ้างอิงหัวข้อ "สรุปสิ่งที่ควรเพิ่มก่อนขึ้น Production" กลุ่ม **สูง** (ข้อ 37, 8, 15–18, 21, 29) เท่านั้น
ยังไม่แตะรายการกลุ่ม **กลาง** และ **ต่ำ**

## 1. คำสั่งเร่งรัดของผู้บริหาร + ติดตามรับทราบ/Escalation (ข้อ 37, WAR051–053)
- เพิ่มสิทธิ์ `manageExecutiveOrders` (Executive) และ `respondExecutiveOrder` (บทบาทผู้ปฏิบัติงานทุกบทบาท) — `js/seed-data.js`
- เพิ่มกลไก `performOrder()` ใน `js/state-machine.js`: สร้างคำสั่ง (`order-create`), รับทราบ (`order-acknowledge`), อัปเดตความคืบหน้า (`order-progress`), ยกระดับ/ทวงถาม (`order-escalate`), ปิดงาน (`order-close`)
- เพิ่ม UI ในหน้า `#/reports`: ผู้บริหารสร้างคำสั่ง ระบุผู้รับ/ความสำคัญ/กำหนดเวลา ผู้รับทราบและอัปเดตสถานะได้ — `js/app.js`

## 2. แยกเลขทะเบียนสำนัก/กอง และเลขทะเบียน กอท. ออกจากเลขหมายศาล (ข้อ 8)
- เพิ่มฟิลด์ถาวร `record.agencyRegisterNo` (บันทึกตอน `agency-register`) และ `record.gotRegisterNo` (บันทึกตอน `got-receive`) แยกจาก `record.warrantNo`
- หน้าตรวจรับหมาย (`review` tab) แสดงเลขทั้ง 3 ชุดพร้อมกันและมีช่องกรอกเลขทะเบียนแต่ละหน่วยงานตอนดำเนินการ

## 3. ผลปฏิบัติการเพิ่มเติม (ข้อ 15–18)
เพิ่ม action ใหม่ในแท็บ "ติดตาม/จับกุม": `enforcement-withdrawn-discovered` (หมายถูกถอน/เพิกถอนระหว่างติดตาม), `enforcement-escaped` (หลบหนีทั่วไป), `enforcement-obstruction` (ถูกขัดขวาง), `enforcement-uncontactable` (ติดต่อเจ้าของสำนวนไม่ได้)
- หลบหนี/ขัดขวาง/ติดต่อไม่ได้ → คงสถานะ "อยู่ระหว่างประสานติดตาม" เหมือนกรณีไม่พบตัว
- หมายถูกถอน/เพิกถอนระหว่างติดตาม → ต้องตรวจเอกสารยืนยัน (เหมือนกรณีเสียชีวิต/ขาดอายุความ) ก่อนเปิดงานตรวจเหตุยุติ

## 4. กรณี WAR010 ตำรวจส่งตรงอัยการโดย ป.ป.ท. ไม่ทราบล่วงหน้า (ข้อ 21)
- เพิ่ม action `enforcement-direct-handover`: บันทึกผลจับกุมและส่งตัวสำเร็จพร้อมกันในรอบเดียว โดยติดป้ายที่มาข้อมูลเป็น `ManualRetroactiveVerification` ("ตรวจสอบย้อนหลัง") ทั้งในผลการติดตามและ Audit

## 5. กรณีคดีโอนไปศาลอื่นตอนรายงานถอนหมาย (ข้อ 29)
- เพิ่ม action `withdrawal-court-transfer`: บันทึกศาลที่รับโอน วันที่ และหลักฐาน ลงใน `record.courtTransfer` พร้อมสร้างเอกสาร `CourtTransferOrder` และแสดงข้อความเตือนในแท็บ "ยุติและถอนหมาย"

## หมายเหตุด้านข้อมูล
- `schemaVersion` ปรับจาก 3 เป็น 4 เพื่อให้ localStorage เดิมของผู้ใช้ถูกสร้างใหม่พร้อมโครงสร้างข้อมูลที่เพิ่ม (`executiveOrders`, ฟิลด์เลขทะเบียนแยก) แทนการ migrate ข้อมูลเก่าซึ่งจะไม่มีฟิลด์เหล่านี้
- ยังไม่ได้เพิ่ม Presenter Scenario ใหม่สำหรับ flow เหล่านี้ (อยู่ในรายการกลุ่ม "ต่ำ" ข้อ 15 ของเช็คลิสต์) และยังไม่ได้แก้หน้าจอบริหารสิทธิ/Reminder อัตโนมัติ (กลุ่ม "กลาง") ตามขอบเขตที่ระบุว่าให้ทำเฉพาะระดับสูงก่อน
